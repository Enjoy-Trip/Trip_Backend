<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.trip.region.mapper.RegionMapper">
	<resultMap type="sidoDto" id="sidoSimple">
		<result column="sido_code" property="sidoCode" />
		<result column="sido_name" property="sidoName" />
	</resultMap>
	
	<resultMap type="sidoDto" id="sidoDetail">
		<result column="sido_code" property="sidoCode" />
		<result column="sido_name" property="sidoName" />
		<collection property="gugunList" column="{sidoCode = sido_code}" select="gugunList" javaType="List" ofType="gugun" />
	</resultMap>
	
	<resultMap type="gugunDto" id="gugun">
		<result column="gugun_code" property="gugunCode" />
		<result column="gugun_name" property="gugunName" />
	</resultMap>

	<select id="sidoList" resultMap="sidoSimple">
		select *
		from sido
	</select>
	
	<select id="sidoDetail" parameterType="Integer" resultMap="sidoDetail">
		select *
		from sido
		where sido_code = #{sidoCode}
	</select>
	
	<select id="gugunList" resultMap="gugun">
		select *
		from gugun
		where sido_code = #{sidoCode}
	</select>
</mapper>