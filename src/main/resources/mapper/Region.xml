<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.trip.region.mapper.RegionMapper">

	<resultMap type="regionDto" id="region">
		<result column="sido_code" property="sidoCode" />
		<result column="sido_name" property="sidoName" />
		<result column="gugun_code" property="gugunCode" />
		<result column="gugun_name" property="gugunName" />
	</resultMap>

	<select id="sidoList" resultMap="region">
		select *
		from sido
	</select>
	
	<select id="gugunList" parameterType="Integer" resultMap="region">
		select s.sido_code sido_code, s.sido_name sido_name, g.gugun_code gugun_code, g.gugun_name gugun_name
		from sido s inner join gugun g
		on s.sido_code = g.sido_code
		where s.sido_code = #{sidoCode}
	</select>
</mapper>